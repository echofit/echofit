public: true
domain: mcp.echofit.ai

service:
  cmd: echofit-mcp serve

env:
  - name: SIGNING_KEY
    secret:
      name: signing-key
      generate: true
  - name: APP_USERS_PATH
    value: "{{SOLUTION_DATA_PATH}}/users"
